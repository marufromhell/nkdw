# NK Declarative Wrapper(WIP)  
Wraps around dnf so you can version control it!  
rhel based only, must be same rhel based distro ex: nobara->nobara/rhel8->rhel8                       
# Install
`pip install nkdw_maru`
